fn main
{
    1 + "blah";      //~ ERROR 3001
    "blah" + 1;      //~ ERROR 3001
    "blah" + "blah"; //~ ERROR 3001
           //^ ERROR 3001

    // only one error because the paren exp recovers to int type
    1 + ( 1 + "blah" );      //~ ERROR 3001

    ret 0;
}
