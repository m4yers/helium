fn main
{
    let a:int = 0;
    let b:int = 0;

    a = b = 5;

    assert!(a == 5);
    assert!(b == 5);

    let c = a = b = 10;

    assert!(a == 10);
    assert!(b == 10);
    assert!(c == 10);
}
