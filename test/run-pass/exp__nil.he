fn main
{
    def Point = { x: int, y: int }

    let a: Point = nil;
    assert! (a == nil);

    let b = Point{ x = 10, y = 20 };
    b = Point { x = 20, y = 30 };
    b = nil;
    assert! (b == nil);

    ret 0;
}
