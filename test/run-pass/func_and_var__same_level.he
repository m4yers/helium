/*
 * blah and sum and in the same static scope and sum can use blah
 */
let
    function main ():int =
        let
            var blah := 1337
            function sum (a:int, b:int):int =
                a + b + blah
        in
            sum (3, 5)
        end
in
     nil
end
